DATABASE_URL="postgresql://postgres:postgres@localhost:5432/diagram?schema=public"
PORT=3000

# Environment
NODE_ENV="local"

# Session Configuration
SESSION_SECRET=""
SESSION_STORE_TYPE="local" # Options: "local" (in-memory) or "redis"
SESSION_COOKIE_NAME="diagram_session" # Custom cookie name (optional)
SESSION_MAX_AGE_HOURS=24 # Session expiry in hours (optional)

# Redis Configuration (only if SESSION_STORE_TYPE=redis)
REDIS_URL="redis://redis:6379"

# JWT
JWT_SECRET=""

# Logto OAuth2 Configuration
LOGTO_APP_ID=""
LOGTO_APP_SECRET=""
LOGTO_ENDPOINT="" # Logto server endpoint
LOGTO_REDIRECT_URI="" # Backend callback URL

# Frontend
FRONTEND_URL=""
